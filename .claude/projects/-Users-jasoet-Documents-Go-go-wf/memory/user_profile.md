---
name: user_profile
description: User is a Go developer building go-wf, a Temporal-based workflow orchestration library
type: user
---

Go developer working on `go-wf`, a generic workflow orchestration library built on Temporal.
Uses Go 1.26+, Go generics, and `github.com/jasoet/pkg/v2` as base library.
Toolchain preferences: `task` for commands, `uv` for Python, `bun` for Node.js, `podman` for containers.
Strict about conventional commits, branch-per-change, and squash merges.
Company policy: NO AI co-authorship in git commits.
